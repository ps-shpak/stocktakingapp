export enum EPaths {
    // NOTE: each must start with '/web', otherwise nginx will route request incorrectly
    MAIN = "/web/",
    USERS = "/web/users",
    LICENSE = "/web/license",
    PROPERTY = "/web/property"
}
