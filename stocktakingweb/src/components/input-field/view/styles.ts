import createStyles from "@material-ui/core/styles/createStyles";

export  const styles = createStyles({
    field: {
        display: "flex",
        width: "100%"
    },
    input: {
        display: "flex",
        padding: "10px 20px"
    }
});
