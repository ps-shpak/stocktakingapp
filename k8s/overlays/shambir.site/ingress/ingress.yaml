apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: stocktaking-ingress
spec:
  tls:
  - hosts:
    - shambir.site
  rules:
  - host: shambir.site
    http:
      paths:
      - path: /
        backend:
          serviceName: stocktakingweb
          servicePort: 80
